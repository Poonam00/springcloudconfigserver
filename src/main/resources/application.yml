spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git: 
         uri: https://github.com/Poonam00/config-repo
         searchPaths: '{application}'
server:
  port: 8888
